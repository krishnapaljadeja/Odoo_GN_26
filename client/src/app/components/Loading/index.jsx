import React from "react";
import "./index.scss";
import { Loader } from "../ui";

const Loading = () => {
  return (
    <div className="Loading">
      <Loader />
    </div>
  );
};

export default Loading;
